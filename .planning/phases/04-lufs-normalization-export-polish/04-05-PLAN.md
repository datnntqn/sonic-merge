---
phase: 04-lufs-normalization-export-polish
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified: []
autonomous: false
requirements: [EXP-03]

must_haves:
  truths:
    - "User can tap Export from MixingStation, enable 'Normalize to -16 LUFS' toggle, tap Export, see 'Exporting & Normalizing...', and receive a shareable file"
    - "User can tap Export from CleaningLab with LUFS toggle on and receive a normalized file"
    - "After sharing from either view, the screen returns to ready state with no stuck progress"
    - "Full test suite passes with no regressions"
  artifacts:
    - path: "SonicMergeTests/LUFSNormalizationServiceTests.swift"
      provides: "All 3 tests GREEN"
    - path: "SonicMergeTests/MixingStationViewModelTests.swift"
      provides: "All 5 tests GREEN including testDismissShareSheetResetsState"
  key_links:
    - from: "ExportFormatSheet LUFS toggle"
      to: "AudioMergerService gainScalar application"
      via: "ExportOptions → MixingStationViewModel.exportMerged → AudioMergerService.export(lufsNormalize:)"
      pattern: "lufsNormalize"
---

<objective>
Human verification of the complete Phase 4 export flows. Automated tests confirm correct service behavior; this plan confirms the UX is correct end-to-end in a running simulator.

Purpose: Confirms toggle appears, progress label changes, share sheet presents and dismisses cleanly, and state resets in both export paths.
Output: Human sign-off or bug list for a gap-closure plan.
</objective>

<execution_context>
@/Users/datnnt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/datnnt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-lufs-normalization-export-polish/04-04-SUMMARY.md
@.planning/phases/04-lufs-normalization-export-polish/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite — confirm all Phase 4 tests GREEN</name>
  <files></files>
  <read_first>
    - .planning/phases/04-lufs-normalization-export-polish/04-04-SUMMARY.md (confirm all prior plans completed)
  </read_first>
  <action>
Run the Phase 4 test suites and confirm zero failures:

```
xcodebuild test \
  -scheme SonicMerge \
  -destination 'platform=iOS Simulator,name=iPhone 16' \
  -only-testing SonicMergeTests/LUFSNormalizationServiceTests \
  -only-testing SonicMergeTests/MixingStationViewModelTests \
  -only-testing SonicMergeTests/AudioMergerServiceTests \
  2>&1 | grep -E "Test Suite|passed|failed|error:"
```

Specific Phase 4 tests that MUST be GREEN before proceeding:
- `LUFSNormalizationServiceTests/testGainScalarForKnownLoudness` — must pass GREEN
- `LUFSNormalizationServiceTests/testGainScalarAlreadyAtTarget` — must pass GREEN
- `LUFSNormalizationServiceTests/testExportWithLUFSEnabled` — must pass GREEN
- `MixingStationViewModelTests/testExportOptionsLUFSFlag` — must pass GREEN
- `MixingStationViewModelTests/testDismissShareSheetResetsState` — must pass GREEN
- All 3 pre-existing `MixingStationViewModelTests` methods — must pass GREEN (no regressions)
- All existing `AudioMergerServiceTests` — must pass GREEN (no regressions)

If any test fails, do NOT proceed to the checkpoint. Fix the regression first.
  </action>
  <verify>
    <automated>xcodebuild test -scheme SonicMerge -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing SonicMergeTests/LUFSNormalizationServiceTests -only-testing SonicMergeTests/MixingStationViewModelTests -only-testing SonicMergeTests/AudioMergerServiceTests 2>&1 | grep -E "Test Suite|passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - Output contains no "failed" lines
    - LUFSNormalizationServiceTests: 3 passed
    - MixingStationViewModelTests: 5 passed
    - AudioMergerServiceTests: all prior tests passed (count unchanged)
  </acceptance_criteria>
  <done>All automated Phase 4 tests pass GREEN. No regressions from prior phases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UX verification — both export paths in simulator</name>
  <files></files>
  <action>
Build and run the app on iPhone 16 Simulator. Verify both export paths according to the steps below.
  </action>
  <what-built>
    Full Phase 4 export polish wired in both MixingStationView and CleaningLabView:
    - LUFS normalization toggle in ExportFormatSheet (default off, persisted across launches via @AppStorage)
    - Dynamic progress title: "Exporting & Normalizing..." when toggle is on, "Exporting..." when off
    - LUFS gain applied via vDSP (WAV path) and AVAudioMix volume (m4a path)
    - Share sheet auto-presents after export completes; state resets on dismissal in both views
    - ActivityViewController completionWithItemsHandler fires for any share sheet dismissal action
  </what-built>
  <how-to-verify>
    Build and run on iPhone 16 Simulator (iOS 17+). Work through both paths:

    **Path A — MixingStation LUFS export:**
    1. Launch SonicMerge. Import at least one audio file.
    2. Tap the Export button (top-right toolbar). ExportFormatSheet slides up.
    3. Confirm sheet shows THREE rows: Format Picker, LUFS Toggle, Export button. Sheet is taller than it was before Phase 4.
    4. Confirm toggle label is exactly "Normalize to -16 LUFS" and subtitle is "Podcast standard (-16 LUFS)".
    5. Confirm toggle is OFF by default (first launch).
    6. Enable the toggle. Tap "Export".
    7. ExportProgressSheet appears. Confirm title is "Exporting & Normalizing..." (not "Exporting...").
    8. Wait for export to complete. Share sheet (UIActivityViewController) presents automatically.
    9. Tap "Cancel" on the share sheet.
    10. MixingStationView should be visible in its normal state. No stuck progress. Tap Export again — ExportFormatSheet appears with toggle still ON (UserDefaults persisted).
    11. Disable the toggle. Tap "Export". Confirm title is "Exporting..." (not "Exporting & Normalizing...").

    **Path B — CleaningLab LUFS export:**
    12. Navigate to CleaningLab. Tap "Denoise Audio" and wait for completion.
    13. Tap the Export button in CleaningLab toolbar.
    14. Confirm the same LUFS toggle row appears in ExportFormatSheet.
    15. Enable LUFS. Tap "Export". Confirm "Exporting & Normalizing..." progress title.
    16. Share sheet presents. Choose "Cancel" or "Save to Files".
    17. CleaningLabView returns to its normal state — no stuck progress, no lingering share sheet.

    **Regression check:**
    18. Export WITHOUT the LUFS toggle (toggle off). Confirm a valid .m4a or .wav file is produced and the share sheet works normally, and state resets after dismissal.

    Known limitations (not bugs):
    - LUFS measurement is ungated. Files shorter than 3 seconds may show less accurate gain scalars — acceptable for v1.
    - m4a exports with crossfade transitions may have slightly different measured loudness than WAV exports due to different gain application paths.
  </how-to-verify>
  <verify>
    <automated>MISSING — this task requires human simulator verification</automated>
  </verify>
  <acceptance_criteria>
    - LUFS toggle row visible in ExportFormatSheet with correct copy and blue tint
    - ExportProgressSheet title changes dynamically based on LUFS toggle state
    - Share sheet auto-presents after export completes (no manual tap needed)
    - State resets after sharing from MixingStationView (no stuck progress, no stale URLs)
    - State resets after sharing from CleaningLabView (same)
    - Toggle state persists across app restarts (UserDefaults)
    - Export without LUFS still works correctly (regression-free)
  </acceptance_criteria>
  <resume-signal>
    Type "approved" if all verification steps pass.
    Or describe specific issues found (e.g., "toggle not appearing", "title not changing", "state not resetting after share") for a gap-closure plan.
  </resume-signal>
  <done>Human has verified full Phase 4 UX in simulator. Export flows work correctly in both MixingStationView and CleaningLabView with and without LUFS normalization.</done>
</task>

</tasks>

<verification>
Task 1 automated tests must be GREEN before the checkpoint. The checkpoint is the final gate for phase completion.
</verification>

<success_criteria>
- Full Phase 4 test suite passes with zero failures
- LUFS toggle visible and functional in both export paths
- ExportProgressSheet title is dynamic
- Share sheet auto-presents and state resets on dismissal in both views
- Toggle state persists across app launches
- Human approves via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/04-lufs-normalization-export-polish/04-05-SUMMARY.md`
</output>
