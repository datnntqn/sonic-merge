---
phase: 06-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SonicMerge/DesignSystem/SonicMergeTheme.swift
  - SonicMerge/DesignSystem/SonicMergeTheme+Appearance.swift
  - SonicMergeTests/SonicMergeThemeTests.swift
autonomous: true
requirements: [DS-01]

must_haves:
  truths:
    - "Light mode shows off-white #FBFBFC background with Deep Indigo #5856D6 accent"
    - "Dark mode shows pure black #000000 background with Deep Indigo #5856D6 accent and Lime Green #A7C957 AI accent"
    - "All new token slots (accentAI, accentGlow, surfaceCard, surfaceGlass) are accessible via @Environment sonicMergeSemantic"
    - "Existing views continue to compile and resolve colors from the updated palette"
  artifacts:
    - path: "SonicMerge/DesignSystem/SonicMergeTheme.swift"
      provides: "Updated ColorPalette with v1.1 hex values, updated Radius.card to 24pt, new Spacing enum"
      contains: "canvasBackground"
    - path: "SonicMerge/DesignSystem/SonicMergeTheme+Appearance.swift"
      provides: "SonicMergeSemantic with 4 new slots: accentAI, accentGlow, surfaceCard, surfaceGlass"
      exports: ["SonicMergeSemantic", "ThemePreference"]
    - path: "SonicMergeTests/SonicMergeThemeTests.swift"
      provides: "Tests verifying v1.1 color values and new token slots"
      min_lines: 50
  key_links:
    - from: "SonicMerge/DesignSystem/SonicMergeTheme+Appearance.swift"
      to: "SonicMerge/DesignSystem/SonicMergeTheme.swift"
      via: "ColorPalette static references"
      pattern: "SonicMergeTheme\\.ColorPalette\\."
---

<objective>
Migrate the color token system from v1.0 to v1.1 "Modern Spatial Utility" palette and add new semantic token slots for SquircleCard, PillButton, and glassmorphism components.

Purpose: Every downstream component (Plans 02, Phase 7-9) needs the correct color tokens and spacing constants before any visual work begins.
Output: Updated SonicMergeTheme.swift, SonicMergeTheme+Appearance.swift, and passing theme tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-design-system-foundation/06-CONTEXT.md
@.planning/phases/06-design-system-foundation/06-UI-SPEC.md

@SonicMerge/DesignSystem/SonicMergeTheme.swift
@SonicMerge/DesignSystem/SonicMergeTheme+Appearance.swift
@SonicMergeTests/SonicMergeThemeTests.swift

<interfaces>
<!-- Existing code the executor must understand -->

From SonicMerge/DesignSystem/SonicMergeTheme.swift:
```swift
enum SonicMergeTheme {
    enum ColorPalette {
        static let canvasBackground = UIColor(red: 0.973, green: 0.976, blue: 0.980, alpha: 1) // #F8F9FA
        static let primaryAccent = UIColor(red: 0, green: 0.478, blue: 1, alpha: 1) // #007AFF
        static let aiAccent = UIColor(red: 88/255, green: 86/255, blue: 214/255, alpha: 1) // #5856D6
        static let primaryText = UIColor(red: 0.110, green: 0.110, blue: 0.118, alpha: 1)
        static let cardSurface = UIColor.white
    }
    enum Radius {
        static let card: CGFloat = 12
        static let chip: CGFloat = 8
    }
}
```

From SonicMerge/DesignSystem/SonicMergeTheme+Appearance.swift:
```swift
struct SonicMergeSemantic {
    var surfaceBase: UIColor
    var surfaceSlot: UIColor
    var surfaceElevated: UIColor
    var accentAction: UIColor
    var accentWaveform: UIColor
    var textPrimary: UIColor
    var textSecondary: UIColor
    var trustIcon: UIColor

    static func resolved(colorScheme:preference:) -> SonicMergeSemantic
    static var fallbackLight: SonicMergeSemantic
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update ColorPalette hex values, add Radius.card 24pt, add Spacing enum</name>
  <files>SonicMerge/DesignSystem/SonicMergeTheme.swift</files>
  <read_first>
    - SonicMerge/DesignSystem/SonicMergeTheme.swift (current palette values to replace)
    - SonicMerge/DesignSystem/SonicMergeTheme+Appearance.swift (references ColorPalette statics)
    - .planning/phases/06-design-system-foundation/06-UI-SPEC.md (authoritative hex values)
  </read_first>
  <behavior>
    - Test: canvasBackground RGBA matches #FBFBFC (r:0.984, g:0.984, b:0.988)
    - Test: primaryAccent RGBA matches Deep Indigo #5856D6 (r:88/255, g:86/255, b:214/255) — per D-03, replaces #007AFF
    - Test: aiAccent RGBA still matches #5856D6 (unchanged)
    - Test: limeGreen RGBA matches #A7C957 (r:167/255, g:201/255, b:87/255)
    - Test: darkCardSurface RGBA matches #0F0F0F (r:15/255, g:15/255, b:15/255)
    - Test: Radius.card == 24
    - Test: Radius.chip == 8 (unchanged)
    - Test: Spacing.xs == 4, Spacing.sm == 8, Spacing.md == 16, Spacing.lg == 24, Spacing.xl == 32
  </behavior>
  <action>
Update SonicMergeTheme.swift with these exact changes:

1. **ColorPalette** — update existing statics and add new ones:
   - `canvasBackground`: change from `UIColor(red: 0.973, green: 0.976, blue: 0.980, alpha: 1)` to `UIColor(red: 251/255, green: 251/255, blue: 252/255, alpha: 1)` — this is #FBFBFC per D-05
   - `primaryAccent`: change from `UIColor(red: 0, green: 0.478, blue: 1, alpha: 1)` to `UIColor(red: 88/255, green: 86/255, blue: 214/255, alpha: 1)` — Deep Indigo #5856D6 per D-03
   - `aiAccent`: keep as-is (already #5856D6)
   - `primaryText`: keep as-is
   - `cardSurface`: keep as `UIColor.white` (light mode card)
   - ADD `limeGreen = UIColor(red: 167/255, green: 201/255, blue: 87/255, alpha: 1)` — Lime Green #A7C957 per D-04
   - ADD `darkBackground = UIColor(red: 0/255, green: 0/255, blue: 0/255, alpha: 1)` — pure black #000000 per D-02
   - ADD `darkCardSurface = UIColor(red: 15/255, green: 15/255, blue: 15/255, alpha: 1)` — near-black #0F0F0F per UI-SPEC
   - ADD `darkTextPrimary = UIColor(white: 0.96, alpha: 1)` — near-white for dark mode
   - ADD `darkTextSecondary = UIColor(white: 0.55, alpha: 1)` — muted for dark mode

2. **Radius** — update card radius:
   - `card`: change from `12` to `24` (DS-02 squircle radius)
   - `chip`: keep `8`

3. **ADD Spacing enum** inside SonicMergeTheme:
```swift
enum Spacing {
    static let xs: CGFloat = 4
    static let sm: CGFloat = 8
    static let md: CGFloat = 16
    static let lg: CGFloat = 24
    static let xl: CGFloat = 32
    static let xxl: CGFloat = 48
    static let xxxl: CGFloat = 64
}
```
  </action>
  <verify>
    <automated>cd /Users/datnnt/Desktop/DatNNT/App/SonicMerge && xcodebuild test -scheme SonicMerge -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing SonicMergeTests/SonicMergeThemeTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SonicMergeTheme.swift contains `static let canvasBackground = UIColor(red: 251/255, green: 251/255, blue: 252/255, alpha: 1)`
    - SonicMergeTheme.swift contains `static let primaryAccent = UIColor(red: 88/255, green: 86/255, blue: 214/255, alpha: 1)`
    - SonicMergeTheme.swift contains `static let limeGreen = UIColor(red: 167/255, green: 201/255, blue: 87/255, alpha: 1)`
    - SonicMergeTheme.swift contains `static let darkBackground = UIColor(red: 0/255, green: 0/255, blue: 0/255, alpha: 1)`
    - SonicMergeTheme.swift contains `static let darkCardSurface = UIColor(red: 15/255, green: 15/255, blue: 15/255, alpha: 1)`
    - SonicMergeTheme.swift contains `static let card: CGFloat = 24`
    - SonicMergeTheme.swift contains `enum Spacing`
    - SonicMergeTheme.swift contains `static let xs: CGFloat = 4`
    - SonicMergeTheme.swift contains `static let xl: CGFloat = 32`
    - SonicMergeThemeTests passes with updated hex assertions
  </acceptance_criteria>
  <done>ColorPalette has v1.1 hex values (#FBFBFC, #5856D6, #A7C957, #000000, #0F0F0F), Radius.card is 24pt, Spacing enum exists with 7 tiers, all theme tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand SonicMergeSemantic with 4 new token slots and migrate light/dark palettes</name>
  <files>SonicMerge/DesignSystem/SonicMergeTheme+Appearance.swift, SonicMergeTests/SonicMergeThemeTests.swift</files>
  <read_first>
    - SonicMerge/DesignSystem/SonicMergeTheme+Appearance.swift (current struct to expand)
    - SonicMerge/DesignSystem/SonicMergeTheme.swift (updated palette from Task 1)
    - SonicMerge/DesignSystem/TrustSignalViews.swift (uses semantic.trustIcon, semantic.surfaceElevated — must remain valid)
    - .planning/phases/06-design-system-foundation/06-UI-SPEC.md (token slot definitions and hex values)
  </read_first>
  <behavior>
    - Test: lightClassic().surfaceBase matches #FBFBFC
    - Test: lightClassic().accentAction matches Deep Indigo #5856D6
    - Test: lightClassic().accentAI matches Lime Green #A7C957
    - Test: lightClassic().accentGlow matches Deep Indigo #5856D6
    - Test: lightClassic().surfaceCard matches #FFFFFF
    - Test: lightClassic().surfaceGlass is #FBFBFC at 0.6 alpha
    - Test: darkConveyor().surfaceBase matches #000000
    - Test: darkConveyor().accentAction matches Deep Indigo #5856D6
    - Test: darkConveyor().accentWaveform matches Deep Indigo #5856D6
    - Test: darkConveyor().accentAI matches Lime Green #A7C957
    - Test: darkConveyor().surfaceCard matches #0F0F0F
    - Test: darkConveyor().surfaceGlass is #000000 at 0.7 alpha
    - Test: resolved(colorScheme: .dark, preference: .system) returns dark palette
    - Test: resolved(colorScheme: .light, preference: .system) returns light palette
    - Test: fallbackLight matches lightClassic values
  </behavior>
  <action>
Update SonicMergeTheme+Appearance.swift:

1. **Add 4 new properties to SonicMergeSemantic struct** (after existing `trustIcon`):
```swift
var accentAI: UIColor       // Lime Green #A7C957 — same both modes
var accentGlow: UIColor     // Deep Indigo #5856D6 — glow shadows, ring borders
var surfaceCard: UIColor    // Card fill — #FFFFFF light / #0F0F0F dark
var surfaceGlass: UIColor   // Glass header tint — #FBFBFC@0.6 light / #000000@0.7 dark
```

2. **Rewrite `lightClassic()`** — per D-01 through D-05:
```swift
private static func lightClassic() -> SonicMergeSemantic {
    SonicMergeSemantic(
        surfaceBase: SonicMergeTheme.ColorPalette.canvasBackground,   // #FBFBFC
        surfaceSlot: SonicMergeTheme.ColorPalette.cardSurface,        // #FFFFFF
        surfaceElevated: UIColor.white,
        accentAction: SonicMergeTheme.ColorPalette.primaryAccent,     // Deep Indigo #5856D6
        accentWaveform: SonicMergeTheme.ColorPalette.primaryAccent,   // Deep Indigo #5856D6
        textPrimary: SonicMergeTheme.ColorPalette.primaryText,
        textSecondary: UIColor(red: 0.235, green: 0.235, blue: 0.263, alpha: 0.6),
        trustIcon: SonicMergeTheme.ColorPalette.aiAccent,             // Deep Indigo #5856D6
        accentAI: SonicMergeTheme.ColorPalette.limeGreen,             // #A7C957
        accentGlow: SonicMergeTheme.ColorPalette.aiAccent,            // Deep Indigo #5856D6
        surfaceCard: SonicMergeTheme.ColorPalette.cardSurface,        // #FFFFFF
        surfaceGlass: UIColor(red: 251/255, green: 251/255, blue: 252/255, alpha: 0.6)
    )
}
```

3. **Rewrite `darkConveyor()`** — per D-02 hard swap:
```swift
private static func darkConveyor() -> SonicMergeSemantic {
    SonicMergeSemantic(
        surfaceBase: SonicMergeTheme.ColorPalette.darkBackground,     // #000000
        surfaceSlot: SonicMergeTheme.ColorPalette.darkCardSurface,    // #0F0F0F
        surfaceElevated: SonicMergeTheme.ColorPalette.darkCardSurface,// #0F0F0F
        accentAction: SonicMergeTheme.ColorPalette.aiAccent,          // Deep Indigo #5856D6
        accentWaveform: SonicMergeTheme.ColorPalette.aiAccent,        // Deep Indigo #5856D6
        textPrimary: SonicMergeTheme.ColorPalette.darkTextPrimary,    // near-white
        textSecondary: SonicMergeTheme.ColorPalette.darkTextSecondary,// 0.55 white
        trustIcon: SonicMergeTheme.ColorPalette.aiAccent,             // Deep Indigo #5856D6
        accentAI: SonicMergeTheme.ColorPalette.limeGreen,             // #A7C957
        accentGlow: SonicMergeTheme.ColorPalette.aiAccent,            // Deep Indigo #5856D6
        surfaceCard: SonicMergeTheme.ColorPalette.darkCardSurface,    // #0F0F0F
        surfaceGlass: UIColor(red: 0, green: 0, blue: 0, alpha: 0.7) // black@0.7
    )
}
```

4. **Update `fallbackLight`** — it calls lightClassic() so it automatically picks up new slots.

5. **Update tests in SonicMergeThemeTests.swift**:
   - Update `canvasBackgroundRGBA_matchesUX01Background` to assert #FBFBFC (r:0.984, g:0.984, b:0.988)
   - Add test `primaryAccent_isDeepIndigo` asserting #5856D6
   - Add test `limeGreen_matchesAIAccent` asserting #A7C957
   - Add test `lightSemantic_hasNewTokenSlots` — resolves light and checks accentAI, accentGlow, surfaceCard, surfaceGlass are non-nil UIColor
   - Add test `darkSemantic_hasPureBlackBase` — resolves dark and checks surfaceBase RGB all == 0
   - Add test `darkSemantic_hasDeepIndigoAccent` — resolves dark and checks accentAction matches #5856D6
   - Add test `darkSemantic_surfaceCardIsNearBlack` — checks surfaceCard RGB ~= 15/255
  </action>
  <verify>
    <automated>cd /Users/datnnt/Desktop/DatNNT/App/SonicMerge && xcodebuild test -scheme SonicMerge -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing SonicMergeTests/SonicMergeThemeTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SonicMergeTheme+Appearance.swift contains `var accentAI: UIColor`
    - SonicMergeTheme+Appearance.swift contains `var accentGlow: UIColor`
    - SonicMergeTheme+Appearance.swift contains `var surfaceCard: UIColor`
    - SonicMergeTheme+Appearance.swift contains `var surfaceGlass: UIColor`
    - SonicMergeTheme+Appearance.swift contains `SonicMergeTheme.ColorPalette.darkBackground` in darkConveyor
    - SonicMergeTheme+Appearance.swift contains `SonicMergeTheme.ColorPalette.limeGreen` in lightClassic
    - SonicMergeThemeTests.swift contains `darkSemantic_hasPureBlackBase`
    - SonicMergeThemeTests.swift contains `lightSemantic_hasNewTokenSlots`
    - All SonicMergeThemeTests pass
    - Project compiles with zero errors (existing views still reference valid semantic properties)
  </acceptance_criteria>
  <done>SonicMergeSemantic has 12 token slots (8 existing + 4 new), light palette uses #FBFBFC/#5856D6, dark palette uses #000000/#5856D6/#A7C957, all theme tests pass, project compiles</done>
</task>

</tasks>

<verification>
1. `xcodebuild test -scheme SonicMerge -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing SonicMergeTests/SonicMergeThemeTests` passes
2. `xcodebuild build -scheme SonicMerge -destination 'platform=iOS Simulator,name=iPhone 16'` compiles with zero errors (existing views still work)
3. grep confirms: `SonicMergeTheme.swift` contains `static let card: CGFloat = 24`
4. grep confirms: `SonicMergeTheme+Appearance.swift` contains `var accentAI: UIColor`
</verification>

<success_criteria>
- v1.1 color palette active: #FBFBFC light background, #000000 dark background, #5856D6 accent both modes, #A7C957 AI accent
- 4 new semantic slots (accentAI, accentGlow, surfaceCard, surfaceGlass) accessible via @Environment
- Radius.card updated to 24pt for squircle components
- Spacing enum with 7 tiers (xs through xxxl) available
- All existing views compile without modification (backward-compatible property names)
- Theme tests pass with updated assertions
</success_criteria>

<output>
After completion, create `.planning/phases/06-design-system-foundation/06-01-SUMMARY.md`
</output>
