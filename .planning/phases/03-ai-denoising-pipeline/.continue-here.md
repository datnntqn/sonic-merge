---
phase: 03-ai-denoising-pipeline
task: UAT re-run (mlpackage blocker now unblocked)
status: in_progress
last_updated: 2026-03-16T15:27:22.117Z
---

<current_state>
Denoising pipeline is now running on simulator (iPhone 17 Pro, iOS 26.2). Three bugs were fixed this session. The app builds and launches. Denoising now starts and runs Core ML inference — waiting for user to confirm whether the chunked inference fix resolves the final error or if there are further issues. UAT tests 5–8 (skipped due to mlpackage blocker) now need to be re-run.
</current_state>

<completed_work>

- Fix 1: `NoiseReductionService.loadModel()` — was searching for `.mlpackage` in bundle, but Xcode compiles it to `.mlmodelc`. Fixed to look for `.mlmodelc` first, fall back to `.mlpackage` for runtime compile.
- Fix 2: `NoiseReductionService.denoise()` — changed from `AsyncStream<Float>` to `AsyncThrowingStream<Float, Error>` so real errors propagate to the user instead of being silently swallowed.
- Fix 3: `NoiseReductionService.runChannelInference()` — model T dimension capped at 6000 frames (~60s). Long audio caused "Size (12212) of dimension (2) is not in allowed range (1..6000)". Fixed with chunked inference: loop over 4000-frame chunks, accumulate `erbMaskFlat` + `coefsFlat` across chunks, then apply ERB mask + deep filtering + iSTFT on the full combined result.
- Build verified: succeeds, `DeepFilterNet3.mlmodelc` confirmed in app bundle.
- App installed and launched on iPhone 17 Pro simulator (id: 6CACDB6E-56A9-4E68-A092-C05F4133AA28).

</completed_work>

<remaining_work>

- Confirm chunked inference fix works end-to-end (user taps Denoise Audio — should succeed now).
- Re-run UAT tests 5–8 (all skipped due to mlpackage blocker):
  - Test 5: Intensity Slider — Fast Re-Blend Without Re-Inference
  - Test 6: A/B Hold-to-Compare with Haptic
  - Test 7: Stale Result Banner
  - Test 8: Export Denoised Audio
- Update 03-UAT.md with results.
- If all pass: run /gsd:verify-work and /gsd:complete-milestone or proceed to next phase.

</remaining_work>

<decisions_made>

- Chose chunked inference (non-overlapping 4000-frame chunks) over streaming/overlap approach for simplicity. GRU state resets at chunk boundaries (~every 40s) causing minor quality degradation, acceptable for v1.
- `chunkMaxFrames = 4000` (well below model limit of 6000) chosen as safe margin.
- `AsyncThrowingStream<Float, Error>` adopted so Core ML errors surface directly in the UI alert.

</decisions_made>

<blockers>

- None currently. All three root causes identified and fixed. Awaiting user confirmation that denoising completes successfully.

</blockers>

<context>
The mlpackage blocker from UAT is now fully resolved. Three layers of bugs were fixed:
1. Bundle lookup (.mlpackage vs .mlmodelc) — model was never loading
2. Silent error swallowing — failures were invisible to the user
3. Input size constraint — model T dimension capped at 6000, long audio exceeded it

Modified files (not yet committed to main):
- SonicMerge/Features/Denoising/NoiseReductionService.swift
- SonicMerge/Features/Denoising/CleaningLabViewModel.swift
</context>

<next_action>
Start with: User confirms Denoise Audio works (or share new error screenshot). If working, run UAT tests 5–8. Update 03-UAT.md. Then /gsd:verify-work.
</next_action>
