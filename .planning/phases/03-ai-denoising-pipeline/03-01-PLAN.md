---
phase: 03-ai-denoising-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SonicMergeTests/NoiseReductionServiceTests.swift
  - SonicMergeTests/WetDryBlendTests.swift
  - SonicMergeTests/ABPlaybackTests.swift
  - scripts/convert_deepfilternet3.py
  - docs/DENOISING_SETUP.md
autonomous: true
requirements: [DNS-01, DNS-02, DNS-03]
must_haves:
  truths:
    - "Test stubs for DNS-01, DNS-02, DNS-03 exist and fail (red) before any implementation"
    - "Developer can follow DENOISING_SETUP.md to produce DeepFilterNet3.mlpackage offline"
    - "coremltools conversion script handles explicit RNN state I/O for iOS 17 compatibility"
    - "All three test files compile in the SonicMergeTests target"
  artifacts:
    - path: "SonicMergeTests/NoiseReductionServiceTests.swift"
      provides: "Failing stubs: testDenoisedFileCreated, testOutputFormatIsValid, testProgressMonotonicallyIncreases"
    - path: "SonicMergeTests/WetDryBlendTests.swift"
      provides: "Failing stubs: testZeroIntensityReturnsOriginal, testFullIntensityReturnsDenoised, testHalfIntensityIsLinearMid"
    - path: "SonicMergeTests/ABPlaybackTests.swift"
      provides: "Failing stub: testPositionPreservedOnSwitch"
    - path: "scripts/convert_deepfilternet3.py"
      provides: "coremltools conversion script with explicit RNN state I/O"
    - path: "docs/DENOISING_SETUP.md"
      provides: "Step-by-step developer prerequisite instructions"
  key_links:
    - from: "SonicMergeTests/NoiseReductionServiceTests.swift"
      to: "NoiseReductionService (not yet created)"
      via: "@testable import SonicMerge"
      pattern: "XCTFail.*not implemented"
    - from: "scripts/convert_deepfilternet3.py"
      to: "Resources/DeepFilterNet3.mlpackage"
      via: "coremltools ct.convert()"
      pattern: "minimum_deployment_target.*iOS17"
---

<objective>
Wave 0: Create failing test stubs for all DNS-01/DNS-02/DNS-03 behaviors and document the mandatory DeepFilterNet3 coremltools conversion the developer must complete before Wave 1 can run.

Purpose: Nyquist compliance — no Wave 1+ task ships without a pre-existing failing test it must turn green. The coremltools script and developer guide ensure the .mlpackage prerequisite is unblocked before Core ML code executes.

Output: Three test files (failing), one Python conversion script, one developer setup guide.
</objective>

<execution_context>
@/Users/datnnt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/datnnt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-denoising-pipeline/03-CONTEXT.md
@.planning/phases/03-ai-denoising-pipeline/03-RESEARCH.md
@.planning/phases/03-ai-denoising-pipeline/03-VALIDATION.md

<interfaces>
<!-- Existing test pattern from SonicMergeTests — follow exactly -->
<!-- PBXFileSystemSynchronizedRootGroup: any .swift file placed in SonicMergeTests/ is auto-included by Xcode. No manual project.pbxproj edits needed. -->

From SonicMergeTests/WaveformServiceTests.swift (reference pattern):
```swift
import XCTest
@testable import SonicMerge

final class WaveformServiceTests: XCTestCase {
    func testSomeFeature() async throws {
        // XCTFail("not implemented") for stub pattern
    }
}
```

From SonicMergeTests/AudioMergerServiceTests.swift (AsyncStream test pattern):
```swift
var collected: [Float] = []
for await progress in stream {
    collected.append(progress)
}
XCTAssertTrue(collected.first! <= collected.last!)
```

<!-- NoiseReductionService does not exist yet — tests import it speculatively; they will fail to compile until Wave 1 creates it. Use XCTFail("not implemented") stubs where necessary to ensure tests FAIL (red) before implementation. -->
<!-- For ABPlaybackTests: AVAudioPlayer cannot be initialized in unit tests without real audio files on disk. Use a mock/subclass or skip init — stub with XCTFail to stay in red. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create failing test stubs — NoiseReductionServiceTests, WetDryBlendTests, ABPlaybackTests</name>
  <files>
    SonicMergeTests/NoiseReductionServiceTests.swift,
    SonicMergeTests/WetDryBlendTests.swift,
    SonicMergeTests/ABPlaybackTests.swift
  </files>
  <action>
Create three XCTest files in SonicMergeTests/. All test methods MUST call XCTFail("not implemented") so the suite is red before any implementation. Do NOT make any test accidentally pass.

**SonicMergeTests/NoiseReductionServiceTests.swift**
- `import XCTest`, `@testable import SonicMerge`
- `final class NoiseReductionServiceTests: XCTestCase`
- `func testDenoisedFileCreated() async throws` — calls `XCTFail("not implemented — DNS-01: Wave 1 implements NoiseReductionService")`
- `func testOutputFormatIsValid() async throws` — calls `XCTFail("not implemented — DNS-01: output must be 48 kHz stereo")`
- `func testProgressMonotonicallyIncreases() async throws` — calls `XCTFail("not implemented — DNS-01: AsyncStream<Float> must yield increasing values 0.0→1.0")`
- Add a comment block at top explaining: "Requires DeepFilterNet3.mlpackage in app bundle. See docs/DENOISING_SETUP.md before running Wave 1."

**SonicMergeTests/WetDryBlendTests.swift**
- `import XCTest`, `@testable import SonicMerge`
- `final class WetDryBlendTests: XCTestCase`
- `func testZeroIntensityReturnsOriginal()` — calls `XCTFail("not implemented — DNS-02: blend(intensity:0.0) must equal original")`
- `func testFullIntensityReturnsDenoised()` — calls `XCTFail("not implemented — DNS-02: blend(intensity:1.0) must equal denoised")`
- `func testHalfIntensityIsLinearMid()` — calls `XCTFail("not implemented — DNS-02: blend(intensity:0.5) must equal (original + denoised) / 2")`
- Note: WetDryBlendTests is pure math — no model or audio file needed. Tests will turn green in Wave 2 (NoiseReductionService blend function).

**SonicMergeTests/ABPlaybackTests.swift**
- `import XCTest`, `@testable import SonicMerge`
- `final class ABPlaybackTests: XCTestCase`
- `func testPositionPreservedOnSwitch()` — calls `XCTFail("not implemented — DNS-03: after switchToOriginal(), originalPlayer.currentTime must match previous denoisedPlayer.currentTime")`
- Add comment: "AVAudioPlayer requires a real audio file — test will use a fixture WAV from SonicMergeTests/Fixtures/ once Wave 3 creates ABPlaybackController."

After writing all three files, run:
`xcodebuild build-for-testing -scheme SonicMerge -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20`

Verify the project COMPILES (even if tests would fail at runtime). If the @testable import fails because NoiseReductionService types don't exist yet, wrap any type references in `#if false` or use only XCTFail stubs with no type dependencies — the tests must compile even when the implementation is absent.
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -scheme SonicMerge -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "(BUILD SUCCEEDED|BUILD FAILED|error:)" | head -10</automated>
  </verify>
  <done>Three test files exist in SonicMergeTests/. Project compiles with BUILD SUCCEEDED. All test methods contain XCTFail stubs. WetDryBlendTests has no external dependencies (compiles standalone).</done>
</task>

<task type="auto">
  <name>Task 2: Write coremltools conversion script and developer setup guide</name>
  <files>
    scripts/convert_deepfilternet3.py,
    docs/DENOISING_SETUP.md
  </files>
  <action>
Create the Python conversion script and developer prerequisite documentation.

**scripts/convert_deepfilternet3.py**

Create the file at the project root under `scripts/`. It does NOT need to run in CI — it is a one-time developer tool. The script must:

1. Install guard: print clear instructions if `coremltools` or `deepfilternet` are not installed
2. Load DeepFilterNet3 from deepfilternet package using `df.enhance.init_df()`
3. Convert to Core ML via `coremltools.convert()` with these REQUIRED settings:
   - `minimum_deployment_target=coremltools.target.iOS17` — prevents MLState generation
   - `compute_precision=coremltools.precision.FLOAT16` — matches ~4.2 MB target size
   - RNN hidden states exported as explicit `ct.TensorType` inputs/outputs — NOT using `ct.State` (MLState is iOS 18+ only)
   - Output name: `DeepFilterNet3.mlpackage`
4. Save to `../SonicMerge/Resources/DeepFilterNet3.mlpackage` (relative to scripts/ directory)
5. Print the generated model's input/output description so the developer can verify tensor names for NoiseReductionService

Add these comments at the top of the script:
```python
# CRITICAL: minimum_deployment_target=iOS17 — MLState requires iOS 18+.
# This script exports RNN hidden states as explicit tensor I/O instead.
# After conversion, inspect DeepFilterNet3.swift (Xcode auto-generates it)
# to verify exact input/output property names before coding NoiseReductionService.
# See: apple.github.io/coremltools/docs-guides/source/stateful-models.html
```

**docs/DENOISING_SETUP.md**

Create a developer prerequisite guide:

```markdown
# Denoising Setup — Developer Prerequisite

Before executing Wave 1+ of Phase 3, the developer must convert DeepFilterNet3 to a
Core ML .mlpackage and add it to the Xcode project. This is a one-time step.

## Prerequisites

- Python 3.9+ with pip
- macOS (coremltools requires macOS for .mlpackage generation)
- ~2 GB disk space for PyTorch model download

## Steps

### 1. Install Python dependencies

    pip install coremltools deepfilternet torch

### 2. Run the conversion script

    cd /path/to/SonicMerge
    python scripts/convert_deepfilternet3.py

The script outputs DeepFilterNet3.mlpackage to SonicMerge/Resources/.

### 3. Add to Xcode project

- In Xcode: File → Add Files to "SonicMerge"
- Select SonicMerge/Resources/DeepFilterNet3.mlpackage
- Ensure "Add to target: SonicMerge" is checked
- Also add to SonicMergeTests target for unit test access

### 4. Verify tensor names

Xcode auto-generates SonicMerge/DeepFilterNet3.swift from the .mlpackage.
Open this file and note the exact property names for:
- Input frame tensor (shape [1, 2, 480])
- Input hidden state tensor(s)
- Output frame tensor (shape [1, 2, 480])
- Output hidden state tensor(s)

Update NoiseReductionService.swift with these exact names.

### 5. iOS 17 Constraint

The model is converted with explicit RNN state I/O (NOT MLState).
MLState requires iOS 18+. Our deployment target is iOS 17+.
This is handled automatically by the conversion script.

## Troubleshooting

- "MLModelError" on iOS 17 device: verify minimum_deployment_target=iOS17 was used
- Build error "no such module 'DeepFilterNet3'": ensure .mlpackage is added to both targets
- Model too large (>10 MB): verify FLOAT16 precision was used (not FLOAT32)
```
  </action>
  <verify>
    <automated>ls /Users/datnnt/Desktop/DatNNT/App/SonicMerge/scripts/convert_deepfilternet3.py && ls /Users/datnnt/Desktop/DatNNT/App/SonicMerge/docs/DENOISING_SETUP.md && echo "FILES EXIST"</automated>
  </verify>
  <done>scripts/convert_deepfilternet3.py exists with iOS 17 explicit state I/O comments and iOS 17 deployment target constraint. docs/DENOISING_SETUP.md exists with 5-step developer guide including tensor name inspection instructions.</done>
</task>

</tasks>

<verification>
- [ ] `xcodebuild build-for-testing -scheme SonicMerge -destination 'platform=iOS Simulator,name=iPhone 16'` returns BUILD SUCCEEDED
- [ ] SonicMergeTests/NoiseReductionServiceTests.swift exists with 3 XCTFail stubs
- [ ] SonicMergeTests/WetDryBlendTests.swift exists with 3 XCTFail stubs (no external deps)
- [ ] SonicMergeTests/ABPlaybackTests.swift exists with 1 XCTFail stub
- [ ] scripts/convert_deepfilternet3.py exists with minimum_deployment_target=iOS17
- [ ] docs/DENOISING_SETUP.md exists with tensor name inspection step
</verification>

<success_criteria>
- All 7 test methods in 3 files exist and compile
- Wave 0 human checkpoint: developer reads DENOISING_SETUP.md, runs conversion script, adds .mlpackage to Xcode before executing Plan 03-02
- WetDryBlendTests is self-contained — no model or audio fixture dependency
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-denoising-pipeline/03-01-SUMMARY.md`
</output>
