// ImportView.swift
// Retired in Phase 2. UI replaced by MixingStationView (Plan 05).
// File retained for Xcode target membership.
import SwiftUI

/// Retired view stub — no longer used as root view.
/// Full Mixing Station UI implemented in MixingStationView (Plan 05).
struct ImportView: View {
    var body: some View {
        EmptyView()
    }
}
