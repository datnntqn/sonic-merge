// MixingStationView.swift
// SonicMerge
//
// Compilation stub — replaced by full implementation in Plan 05.
// Satisfies SonicMergeApp.swift WindowGroup reference so the app target compiles
// and MixingStationViewModelTests can run.
import SwiftUI

/// Placeholder view — Plan 05 provides the full Mixing Station UI.
struct MixingStationView: View {
    @Environment(MixingStationViewModel.self) private var viewModel

    var body: some View {
        Text("Mixing Station — Coming Soon")
    }
}
