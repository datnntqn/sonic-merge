// ImportViewModelTests.swift
// Retired in Phase 2. ImportViewModel functionality migrated to MixingStationViewModel.
// Tests for import pipeline are now covered by MixingStationViewModelTests.
// File retained for Xcode target membership.
import Testing

struct ImportViewModelTests {
    @Test func importViewModelRetired() {
        // ImportViewModel retired in Phase 2 — import logic in MixingStationViewModel.
        #expect(true)
    }
}
